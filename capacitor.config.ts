import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'id.mahapalanarotama.app',
  appName: 'Mahapala Narotama App',
  webDir: 'client/dist'
};

export default config;
