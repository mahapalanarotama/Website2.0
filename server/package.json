{
  "scripts": {
    "build": "powershell -Command \"if (!(Test-Path -Path 'dist/server/public')) { New-Item -ItemType Directory -Path 'dist/server/public' } ; Copy-Item ../client/dist/* dist/server/public/ -Recurse -Force\"",
    "start": "node dist/server/index.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "firebase-admin": "^13.4.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
