{
  "scripts": {
    "build": "npm run build --workspace ../client && tsc && mkdir -p dist/server/public && cp -rf ../client/dist/* dist/server/public/",
    "start": "node dist/server/index.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "firebase-admin": "^13.4.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
